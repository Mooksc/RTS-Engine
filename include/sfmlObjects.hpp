sf::RenderWindow window(sf::VideoMode(windowSizeX,windowSizeY), "Game Test");

sf::Color black = sf::Color::Black;

sf::Event event;

sf::Texture texture;

sf::Sprite sprite;

sf::Font overworldFont;

sf::Font cityFont;

sf::Font font;

sf::Clock overworldClock;
